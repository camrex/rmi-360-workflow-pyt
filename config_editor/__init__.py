"""RMI 360 Config Editor — standalone, comment-preserving config.yaml editor.

Runs OUTSIDE the ArcGIS Pro Python environment with its own optional dependencies
(see config_editor/requirements.txt). The `core` subpackage is GUI-agnostic and
arcpy-free; a pywebview GUI layers on top of it.
"""

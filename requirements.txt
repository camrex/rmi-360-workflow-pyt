# Requirements compatible with ArcGIS Pro 3.4.0+ base Python environment
# Versions are intentionally left unpinned to align with Esri's bundled packages

pyyaml          # YAML parsing (used for config files)
boto3           # AWS SDK for Python
botocore        # Core dependency for boto3
keyring         # Secure credential storage
numpy           # Numerical computing
jinja2          # Templating for reports or config generation
pytest          # Unit testing
matplotlib      # Plotting and visualization

# Documentation dependencies (optional - only needed for building docs)
sphinx>=4.0     # Documentation generator
sphinx_rtd_theme>=1.0  # Read the Docs theme
